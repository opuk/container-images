[main]
vardir = /var/lib/puppet
logdir = /var/log/puppet
rundir = /var/run/puppet
ssldir = $vardir/ssl

[agent]
pluginsync      = true
report          = true
ignoreschedules = true
daemon          = false
ca_server       = satellite.lab.kupo.se
certname        = container1.lab.kupo.se
environment     = testing
server          = satellite.lab.kupo.se


